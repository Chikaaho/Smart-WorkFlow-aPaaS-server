# I6 引导测试（I6G1a → I6G1b → I6G1c）按设计共享同一文件 H2 库并模拟跨 JVM 重启：
# G1a 建表并持久化失败投递，G1b/G1c 在新上下文里恢复。类执行顺序必须稳定，
# 否则文件库在 G1a 建表前就被读取（BadSqlGrammar）。此处固定为类名升序，
# 不依赖 surefire 的文件系统顺序。
junit.jupiter.testclass.order.default=org.junit.jupiter.api.ClassOrderer$ClassName
